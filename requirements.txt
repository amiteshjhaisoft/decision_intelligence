# ---------- Core UI ----------
streamlit>=1.34
pyyaml>=6.0.1

# ---------- Optional: file parsing / uploads (safe to keep) ----------
pandas>=2.2
openpyxl>=3.1
pypdf>=4.2
python-docx>=1.1
python-pptx>=0.6.23

# ---------- LLM (Claude) ----------
# Uses environment variable ANTHROPIC_API_KEY
anthropic>=0.20,<1.0

# ---------- Vector DB (Milvus) ----------
pymilvus>=2.4,<3.0

# ---------- Retrieval / embeddings (RAG plumbing) ----------
sentence-transformers>=2.6,<3.0
faiss-cpu>=1.8.0.post1; platform_system != "Windows"
# If on Windows, prefer this fork or build FAISS separately:
# faiss-cpu-windows>=1.8.0.post1; platform_system == "Windows"

# ---------- Optional: LangChain scaffolding (if you plan to use it) ----------
langchain-core>=0.2,<0.3
langchain-community>=0.2,<0.3
langchain-text-splitters>=0.2,<0.3

# ---------- Misc (quality of life) ----------
python-dotenv>=1.0
requests>=2.32
